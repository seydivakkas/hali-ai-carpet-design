# User Guide

See `HALI_AI_CARPET_DESIGN_MASTER_BUILD_SPEC.md` for full specification.

## Quick Start

```bash
# Bootstrap
uv venv --python 3.11
uv sync --all-extras
uv run carpet-designer doctor

# Launch UI
uv run carpet-designer serve
```
