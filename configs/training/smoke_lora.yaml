training:
  resolution: 512
  train_batch_size: 1
  gradient_accumulation_steps: 1
  learning_rate: 0.0001
  max_train_steps: 5
  rank: 4
  alpha: 4
  mixed_precision: fp16
  gradient_checkpointing: true
  use_8bit_adam: false
  checkpointing_steps: 5
  validation_steps: 5
  seed: 42

lora:
  target: unet
  train_text_encoder: false
  adapter_name: carpet_smoke_test

note: >
  Smoke training configuration with minimal steps for hardware validation.
  Not intended to produce quality results.
